FROM mysql:8.0.30

# 设置环境变量
ENV MYSQL_ROOT_PASSWORD=Devops@2021 \
    MYSQL_DATABASE=daifu \
    MYSQL_USER=admin \
    MYSQL_PASSWORD=admin_password

# 拷贝初始化 SQL 文件
COPY ./doc/db.sql /docker-entrypoint-initdb.d/

#docker build -t arvingrep/mysql:8.0.30 -f mysql-dockerfile .
